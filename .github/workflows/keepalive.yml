name: Keep Vercel Warm

on:
  schedule:
    - cron: "*/5 * * * *" # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel API
        run: |
          curl -s https://perks-marketplace-backend.vercel.app/health > /dev/null
